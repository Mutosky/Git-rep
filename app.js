let colors = ['red', 'blue'];
let result = colors[1] + ' is ' + colors.length;
console.log(result);

function Welcome(FirstName, LastName, Class, age) {
    let tame = FirstName + ' ' + LastName + ' is in ' + Class + ' and he is ' + age + ' years old'; 
    console.log(tame);
}
Welcome('Mutolib', 'Epemolu', 'Year 2 Computer Craft', 15);